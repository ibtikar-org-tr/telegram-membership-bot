{
  "name": "telegram-membership-bot",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.9.10"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "wrangler": "^4.4.0"
  }
}
